package com.spring.project.development.voler.repository.cusService;

import com.spring.project.development.voler.entity.cusService.CusSerTargetComment;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CusSerTargetCommentRepository extends JpaRepository<CusSerTargetComment, String> {
}
